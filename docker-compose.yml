services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app

  streamlit:
    build: ./ui
    ports:
      - "8501:8501"
    volumes:
      - ./ui:/app
    depends_on:
      - api

